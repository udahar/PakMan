[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pakman-ollama-ops"
version = "0.1.0"
description = "Shared Ollama instance control primitives for Windows hosts"
requires-python = ">=3.9"

