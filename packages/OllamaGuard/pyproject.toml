[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pakman-ollama-guard"
version = "0.1.0"
description = "Host bridge and launcher for Ollama stall recovery"
requires-python = ">=3.9"

