# Recursive Context

Recursive context management for deep conversations.

## Usage

```python
from recursive_context import ContextManager

ctx = ContextManager()
ctx.push("User message")
summary = ctx.summarize()
```
