# Evaluation Framework

Framework for evaluating AI model outputs.

## Usage

```python
from eval_framework import evaluate

result = evaluate(prediction, ground_truth, metrics=["accuracy", "f1"])
```
