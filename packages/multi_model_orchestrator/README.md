# Multi-Model Orchestrator

Orchestrates multiple AI models working together.

## Usage

```python
from multi_model_orchestrator import Orchestrator

orch = Orchestrator()
result = orch.run("Complex task", models=["gpt4", "claude"])
```
