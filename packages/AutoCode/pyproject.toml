[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "pakman-autocode"
version = "0.1.0"
description = "Automated code generation pipeline. Task â†’ brief â†’ code â†’ validate â†’ commit."
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
keywords = ["ai", "pakman", "coding"]
dependencies = []

[tool.setuptools.packages.find]
where = [".."]
include = ["AutoCode*"]
