# Model Router

Intelligent routing of requests to different AI models.

## Usage

```python
from model_router import Router

router = Router()
response = router.route(" Which model should handle this?")
```
